def analytics_menu(user_id: int) -> None:
    """
    Display the analytics submenu for a given user.

    Args:
        user_id (int): The ID of the user whose habits are analyzed.

    Returns:
        None
    """
    # implementation unchanged
    ...
